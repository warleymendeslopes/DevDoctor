# Backlog de conteúdo (GitHub / LinkedIn / DEV)

Série sugerida em três publicações, alinhada às releases e à transparência sobre limitações.

## Post 1 — Lançamento honesto (v1 → v2)

- **Título de trabalho:** “Lancei um CLI que explica erros de terminal com IA — e o que aprendi ao torná-lo público.”
- **Pontos:** problema (tempo perdido com stderr), o que o DevDoctor faz, o que **não** faz (não é agente), link para o repo e npm.
- **CTA:** experimentar `devdoctor setup` + `npm install -g devdoctor-cli`, abrir issue com feedback.

## Post 2 — Privacidade e modelos (Ollama vs nuvem)

- **Título de trabalho:** “Como reduzir vazamento de dados ao usar IA no terminal (sanitização, preview, Ollama).”
- **Pontos:** o que é enviado ao provedor, `--preview`, `DEVDOCTOR_CONFIRM_SEND`, comparação breve GPT/Gemini vs modelo local.
- **CTA:** configurar Ollama e quando ainda faz sentido usar API paga.

## Post 3 — Casos reais antes/depois

- **Título de trabalho:** “10 minutos de terminal: antes só o stack trace, depois com contexto mínimo.”
- **Pontos:** um erro real (jest, tsc, npm), screenshot ou asciinema, menção a `devdoctor explain` em comandos longos.
- **CTA:** contribuir com padrões de erro no parser ou issues.

## Formato

- Manter tom transparente sobre v1/v2 e roadmap.
- Priorizar **educação** sobre autopromoção vazia.
